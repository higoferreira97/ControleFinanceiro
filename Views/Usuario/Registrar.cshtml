@{
    Layout = "_LayoutLogin";
}
<div class="cadastro-box">
    <h1>Cadastre-se</h1>
    <form method="post" asp-controller="Usuario" asp-action="Cadastrar">
        <input type="text" name="nome" placeholder="Nome" required>
        <input type="email" name="email" placeholder="Email" required>
        <input type="password" name="senha" placeholder="Senha" required>
        <button type="submit">Criar contar</button>
    </form>
   @*  <button type="button" onclick="window.location.href='@Url.Action("Index", "Usuario")'">Voltar</button> *@
    <a class="btn-voltar" type="button" asp-action="Index" asp-controller="Usuario">Voltar</a>
</div>









@* <h2>Cadastre-se</h2>
<link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
<form class="login" method="post" asp-controller="Usuario" asp-action="Cadastrar">
    <div class="col-md-6 justify-content-center">
        <label for="inputNome4" class="form-label">Nome</label>
        <input type="text" class="form-control" id="inputNome" name="nome" required>
        <label for="inputNome4" class="form-label">Email</label>
        <input type="email" class="form-control" id="inputEmail4" name="email" required>
        <label for="inputPassword4" class="form-label">Senha</label>
        <input type="password" class="form-control" id="inputPassword4" name="senha" required>
    </div>

    <div class="d-flex justify-content-center">
        <button class="btn btn-primary" type="submit">Cadastrar</button>
        <a asp-action="Index" asp-controller="Usuario" class="btn btn-voltar">Voltar</a>
    </div>




    @if (!ViewData.ModelState.IsValid)
    {
        <div style="color: red;">
            @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
            {
                <p>@error.ErrorMessage</p>
            }
        </div>
    }
</form>
 *@




























@* @model UsuarioModel

@if (!ViewData.ModelState.IsValid)
{
    <div style="color: red;">
        @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
        {
            <p>@error.ErrorMessage</p>
        }
    </div>
}


<div class="cadastro" "col-md-6 justify-content-center">
       <h1>Cadastro de Usuários</h1>

    <form asp-action="Cadastrar" asp-controller="Usuario" method="post">
         <div class="mb-3">
            <label for="nome" class="form-label">Nome:</label>
            <input type="text" class="form-control" id="nome" asp-for="Nome">
        </div>
        <div class="mb-3">
            <label for="email" class="form-label">Email:</label>
            <input type="email" class="form-control" id="email" asp-for="Email">
        </div>
        <div class="mb-3">
            <label for="senha" class="form-label">Senha</label>
            <input type="password" class="form-control" id="senha" asp-for="Senha">
        </div>
        <div class="d-flex justify-content-center">
            <button type="submit" class="btn btn-primary w-25 mx-2">Cadastrar</button>
            <a asp-action="Index" asp-controller="Usuario" class="btn w-25">Voltar</a>
        </div>
    </form> *@
    